package net.dark.spv_addon.mixins;

/**
 * Player movement mixin temporarily disabled to avoid compilation issues
 * Movement modification will be re-implemented once core system is working
 */
public class PlayerMovementMixin {
    // Temporarily empty to avoid mixin injection errors
    // Will be re-implemented with proper method targeting
}
